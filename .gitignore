# ---------------------------------------------------------------- heavy: never pushed
# trained models (rebuild with scripts/run_atlas.sh or the train stage)
checkpoints/**/*.pt
checkpoints/**/*.pth
# backtracked anchor sets, regenerated by the atlas stage
output/**/anchors/
*.npz
# per-GPU partial results before merge, logs, hydra run dirs
output/_parts/
output/_logs/
output/hydra/
output/**/hydra/
# pre-refactor code and results
_legacy/
venv/
.venv/

# ---------------------------------------------------------------- python / editor noise
__pycache__/
*.pyc
*.egg-info/
build/
dist/
.ipynb_checkpoints/
.DS_Store
.vscode/
.idea/

# ---------------------------------------------------------------- kept (small, useful to share):
#   checkpoints/**/manifest.json     what was trained, hallucination rates, step counts
#   output/**/*.{json,csv,tex,txt}   results, summaries, tables
#   visualization/**/*.{png,pdf}     heatmaps, curves, seed maps
# To also drop figures from the repo, uncomment:
# visualization/**/*.pdf
# visualization/**/*.png
