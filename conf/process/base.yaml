# process/base - general process settings shared by every process

T_train: 500  # steps for the learned sampler (150 was too coarse: at d>=32 even the EXACT score hallucinates, see README)
T_true: 200   # steps for the analytic (exact-field) backtrack
