# @package sweep
# the paper grid: every (d, K) cell, the exact-score backtrack at four T values
d: [2, 4, 8, 16, 32]
K: [2, 4, 8, 16]
T_train: 150                          # steps of the learned sampler (the ground truth)
T_atlas: [100, 200, 500, 1000]        # steps of the exact-score backtrack / analytic control
